import * as React from 'react';
import { Input } from '@devoinc/genesys-ui';

export const InputExample = () => (
  <Input
    id="input-example"
    label="Input label"
    helper="This is the helper for the input."
    placeholder="Enter text..."
  />
);
